import indexedReferences from "./indexed";
import insertCitations from "./note";
import label from "./label";

const CITATION_SIZE = {
    LARGE: "large",
    SMALL: "small",
    ONLY_INDEX: "index"
  }

export {indexedReferences, insertCitations, label, CITATION_SIZE}