import React from 'react'
import { Typography } from '@mui/material'

export default function Sequence({sequence, motifs}) {
  return (
    <div style={{marginTop: "10px"}} >
      <Typography variant='h4' >Sequence</Typography>
    </div>
  )
}
